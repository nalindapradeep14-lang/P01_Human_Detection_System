# Install with:   pip install -r requirements.txt
 
flask>=3.0.0              # Web framework + MJPEG streaming
ultralytics>=8.0.0        # YOLOv8 (auto-installs PyTorch CPU build)
opencv-python>=4.8.0      # Webcam capture, frame encoding
numpy>=1.24.0             # Array ops
 